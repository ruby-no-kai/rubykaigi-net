# Per-interface IPv6Forwarding= only writes conf.<iface>.forwarding, but the
# kernel's ip6_forward() gates on net.ipv6.conf.all.forwarding. Setting
# IPv6Forwarding= here in networkd.conf controls the 'all' sysctl.
# https://github.com/systemd/systemd/issues/33414
[Network]
IPv6Forwarding=yes
